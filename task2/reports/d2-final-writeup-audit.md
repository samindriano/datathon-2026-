# Task 2 Final Writeup Audit

## Verdict

`READY` for delivery at MAIN commit `feca95c`.

The committed PDF is a readable two-page A4 document, its content matches the
immutable final-model evidence, its binary hash and Git handling are exact,
and the local source/build path reproduces the same text and rendered pages
without network or external data. Direct human confirmation establishes that
the registered team name is exactly `Enter Your Team Name` and authorizes the
compact filename token `EnterYourTeamName`; the naming is intentional and
compliant. No model, validation, notebook, Kaggle session, or submission slot
was changed or used by this audit.

## Scope

- MAIN commit: `feca95c4d8798fb20b701aeceb04d2e77343566c`.
- PDF: `output/pdf/EnterYourTeamName_Task2_Writeup.pdf`.
- Source: `task2/reports/EnterYourTeamName_Task2_Writeup.md`.
- Builder: `task2/reports/build_task2_writeup.py`.
- Audit branch/worktree: `codex/audit-d2-writeup`, checked out directly from
  `feca95c`.

Review was limited to the Task 2 writeup and its delivery consistency. The
other files present in the MAIN commit were read only as supporting evidence.

## PDF contract and visual review

- SHA-256 is exactly
  `fcfcc27858b4cca7659ef85327f2eaf9a85e01f92e6002c50e68984023631db3`.
- Git blob hash in the worktree matches the committed blob
  `64eaa6b4b1b21b03c29d47ffa1898d208dbe2eed`.
- `.gitattributes` contains `*.pdf binary`; `git check-attr` reports `diff`,
  `merge`, and `text` unset, and the commit numstat records binary `-/-`.
- The PDF is unencrypted, has two A4 pages, and therefore satisfies the
  maximum-three-page rule.
- Both pages were rendered at 150 DPI and visually inspected. Text, tables,
  formula, headings, footer, and page numbers are legible; no clipping,
  overlap, broken glyph, black square, or off-page content is visible.

## Content consistency

The writeup accurately describes the final `d2-e002-metarank` implementation:

- five target-disjoint, category-balanced folds with seed `20260719`;
- training-fold-only candidate/frequency construction;
- fixed `2 * category Jaccard + 1 * title-token Jaccard` scoring;
- deterministic current frequency, global frequency, and article-ID tie-break;
- training global-mode fallback for unseen current articles;
- official titles/categories only, without screenshot OCR, external data,
  external weights, network calls, or APIs.

All reported E002 values match immutable metrics and the independent audit:

- baseline mean/worst/std: `0.261333 / 0.255000 / 0.005195`;
- E002 mean/worst/std: `0.285333 / 0.278333 / 0.006142`;
- absolute gain `+0.024000`, five of five fold wins, and exact fold vector;
- current-unseen `1,797` rows at `0.123539` for both methods;
- category-OOD `625` rows improving `0.280000` to `0.315200`;
- 449 test predictions, top share `0.275167`, and runtime about 12 seconds;
- canonical numeric prediction SHA-256
  `292bb1567ac81cd70b87b1f4730468830640388919b72126aef69f198e9d1ba0`.

The E010 row matches its immutable independent audit: mean `0.255556`, zero of
five wins against E002, and `REJECT / DO NOT SUBMIT`. The other challenger
values match the frozen experiment registry. The notebook statement matches
clean delivery commit `3b28567`, which is an ancestor of `feca95c` and passed
14 portability/submission tests plus isolated smoke reproduction.

The first submission's public accuracy `0.321` matches the slot-1 registry and
is correctly described as diagnostic because public leaderboard coverage is
30%. The document does not use public score as the sole selection basis. Its
two-page length, actual-final-method description, and delivery framing are
consistent with the project rule requiring at most three pages and delivery by
20 July 2026 at 23:59 WIB.

## Naming

The filename follows the authorized compact convention
`EnterYourTeamName_Task2_Writeup.pdf`; the visible title/footer and PDF author
use the registered form `Enter Your Team Name`. The `Task2` token matches the
competition task, and notebook/writeup naming is internally consistent.

This conclusion relies on the direct human confirmation relayed by MAIN that
`Enter Your Team Name` is the exact registered team name and that
`EnterYourTeamName` is the authorized filename token.

## Rebuild verification

The builder imports only local Python code plus `reportlab` and `pypdf`; it
reads the tracked Markdown source and writes the requested local output. It has
no network, credential, competition-data, model, notebook, or external-weight
dependency.

Using the bundled dependency runtime in isolated Python mode produced a valid
two-page PDF twice. ReportLab-generated byte hashes vary because PDF metadata
and document identifiers are regenerated, so the rebuild files are not
byte-identical to the frozen delivery binary. This does not affect the frozen
PDF hash: extracted text was exact, and the rendered PNG for each rebuilt page
was pixel-byte-identical to the corresponding committed-PDF render.

## Delivery recommendation

Preserve commit `feca95c` writeup artifacts unchanged. MAIN may hand
`output/pdf/EnterYourTeamName_Task2_Writeup.pdf` to the human Submission
Manager for delivery. No additional Kaggle submission or notebook execution is
needed for this writeup audit.
